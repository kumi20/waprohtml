import { HotkeyListenerDirective } from './hotkey-listener.directive';

describe('HotkeyListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new HotkeyListenerDirective();
    expect(directive).toBeTruthy();
  });
});
